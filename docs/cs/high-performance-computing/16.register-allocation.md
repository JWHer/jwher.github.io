---
title: 'Register Allocation'
draft: true
---

# Register Allocation

## Register Allocation
레지스터의 수는 제한되어 있음.
적은 수의 레지스터를 사용해 많은 변수를 할당하는 것이 중요.

## Live Variables

A variable 𝑥is live at a point 𝑝if the value of 𝑥at 𝑝could be used along some path in the flow graph starting at 𝑝

어떻게 컴파일러는 알아내는가?

레지스터는 live range가 겹치는 변수의 수만 있으면 됨.

SPEC benchmark
- FPPP: 거의 모든 변수의 live range가 프로그램 끝날때까지

## Interference Graph

## Graph Coloring

np hard문제

K-coloring이 안되면 메모리에 저장(spilling)

예시)
G 그래프가 m 노드가 K 이웃보다 적게 가지고 있다고 가정.
G-m 그래프가 K-colorable이면, G도 가능

더이상 K 이웃보다 적은 노드가 없으면, 노드 하나를 spill후 반복


컴파일러가 사용한 레지스터 개수 반환.
=> 코드의 live range를 줄여 컴파일러가 잘 찾도록 해줄 수 있음
- 변수 이름 바꾸기, ...

## Chaitin’s Algorithm

```
Assume that our machine has K registers
• (Build) Construct the interference graph (G)

• (Simplify) Repeatedly remove nodes of degree less than K and push it on a stack. 
• (Spill) If the simplification step produces a graph that has nodes only of significant degree
(nodes of degree >= K), mark some node for spilling and push it on the stack. Continue simplification

• (Select) Rebuild the original graph by repeatedly adding a node from the top of the stack and select a color for the node. If it is not a potential spill node, there is always a color for the node. If the node is a potential spill node and there is no color available, it becomes an actual spill

• (Start over) If there were any actual spills, rewrite the code and goto step 1
```

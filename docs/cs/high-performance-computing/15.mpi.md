---
title: 'MPI'
draft: true
---

# MPI

프로세서간 통신. single node에서도 사용가능

## SMP

Symmetric multiprocessing or shared-memory multiprocessing (SMP)

하나의 노드 안, 멀티코어

numa?

## MPI

Message Passing Interface

De facto standard for parallel computing
- Industry-standard

Portability
- Supported on virtually all HPC platforms

Performance
- So far, the best
- High performance and scalability

Rich functionality
- MPI 1.1 – 125 functions
- MPI 2 – 152 functions

20여개쯤 알면 된다..

## Programming Model

MPI runs on virtually any hardware platform:
- Distributed Memory
    멀티노드
- Shared Memory
    싱글노드? 멀티코어
- Hybrid
    멀티노드 + 멀티코어

The MPI programming model clearly remains a distributed memory model

All parallelism is explicit

Message passing programming model 
여러 프로세스간 통신 모델. 대부분 SIMD(SPMD, Single Program Multiple Data)

MPI is for communication among processes 
- Each process has a separate address space

Inter-process communication consists of  
- Synchronization
- Movement of data from one process’s address space to another’s

The exchange of data is cooperative between processes
- Data is explicitly sent by one process and received by another
- Point-to-point communications
- Any change in the receiving process’s memory is made with the receiver’s explicit participation

## Hello world

```
#include “mpi.h”
#include <stdio.h>

int main(int argc, char **argv)
{
    int my_rank, size;

    MPI_Init(&argc, &argv);
    MPI_Comm_rank(MPI_COMM_WORLD, &my_rank);
    MPI_Comm_size(MPI_COMM_WORLD, &size);
    printf("Hello, I am %d of %d\n",
            my_rank, size);
    MPI_Finalize();
    return 0;
}
```

OS에서 스케줄링(CFS, EEVDF)

## Running MPI Programs

```bash
mpicc -o hello hello.c
mpirun np 4 ./hello
```

4 Processor지만, 꼭 4개가 필요하진 않음.

## MPI Naming Conventions

All names have MPI_ prefix

## Error Handling

## Environment

MPI_COMM_WORLD

MPI_COMM_SIZE

MPI_COMM_RANK

tensor와 matrix의 rank 차이?

## MPI Initialization

int MPI_Init

## Termination

int MPI_Finalize(void)

## MPI Communications

- Point-to-point communications
- Collective communications

## MPI Data Type

CHAR가 sign/unsign인지 컴파일러에 따라 다름
MPI_BYTE는 MPI_CHAR의 unsigned. 

## Blocking Send
```
int MPI_Send(void *buf, int count, MPI_Datatype datatype, int dest, int tag, MPI_Comm comm)
```

MPI_Send() is blocking
- When this function returns, the data has been delivered to the system
- User can safely access or overwrite the send buffer
- The message may not have been received by the destination process

## Blocking Receive
```
int MPI_Recv(void *buf, int count, MPI_Datatype datatype, int source, int tag, MPI_Comm comm, MPI_Status *status)
```

MPI_Recv() is blocking
- Waits until a matching message is received from the system
- After it returns, the data is in the buffer and ready for use

Receiving fewer than count occurrences of datatype is OK, but receiving more is an  error

## Example

```cpp
// Send an array from process 0 to 1.
int main(int argc, char **argv) {
    int rank, size;
    double a[10];
    int tag = 1001;
    MPI_Status status;

    MPI_Init(&argc, &argv);
    MPI_Comm_size(MPI_COMM_WORLD, &size);
    MPI_Comm_rank(MPI_COMM_WORLD, &rank);

    if (rank == 0) {
        for (int i=0; i<10 ; i++)
            a[i] = i;
        MPI_Send(a, 10, MPI_DOUBLE, 1, tag, MPI_COMM_WORLD);
    } else if (rank == 1) {
        MPI_Recv(a, 10, MPI_DOUBLE, 0, tag, MPI_COMM_WORLD, &status);
    }
    MPI_Finalize();
}
```

time complexity는 cache에 영향을 받아...

## Deadlocks

Must be careful to avoid deadlock
- Carefully sequence all messages

blocking send/recv이기 때문..

## Buffering

## Communication Modes for Send
MPI_Send, MPI_Bsend, MPI_Ssend, MPI_Rsend, MPI_Recv

## Delivery Order

문서를 볼것

## Send-receive

데드락을 피하기 위해

## Non-blocking Communication

•The sender proceeds even if the message has not been sent
•The receiver proceeds even if the message has not been received
•Either can check on the status of the message
•It avoids idle time and (some) deadlocks
•Overlap computation with communication

•Split communication operations into two parts
- Initiates the operation (non-blocking)
- Waits for the operation to complete

## Collective Communications

## Computing 𝝅

4/(1+x^2)의 적분값. 구간을 두고 1/n씩 함



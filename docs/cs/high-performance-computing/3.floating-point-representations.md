---
title: '과학적 표기법'
draft: true
---

$\pi$를 계산하려면? 무한이 많은 시간이 필요
=> 3.14로 근사

마찬가지로? 실수도 근사
이전에는 vender마다 근사 방법이 달랐음
=> IEEE 754 floating point

## 과학적 표기법
$m \times b^e$  
• 𝑚은 실수로서 가수(假數, mantissa) 또는 계수(係數, coefficient)
• 𝑒는 정수로서 지수(指數, exponent)
• 𝑏는 양의 정수로서 기수(基數, base)
• 소수점의 위치는 고정되어 있지 않음

## 정규화된 과학적 표기법
가수 m의 값을 1 <= m < b으로 한정

8자리로 표현할 수 있는 수 차이
(소수점 아래 자릿수가 2 개)
0.00 에서 999999.99

𝑚0. 𝑚1𝑚2𝑚3𝑚4𝑚5 × 10𝑒1𝑒2 형태의 부동소수점 표현
0.00000 × 10^00 에서 9.99999 × 10^99

유효숫자: 제거하면 정확도가 떨어지는 수
Nan: Not a number

정밀도
- 최근엔 반정밀도 + 단정밀도인 복합 정밀도 사용

근사시 가장 가까운 표현으로
라운딩: 더 작은 자릿수를 가진 값으로 주어진 수를 어림잡는 작업
라운딩 오차(rounding error): 원래의 값과 라운딩한 값 간에 차이

라운딩 규칙:
가장 가까운 쪽으로 하되 비기면 짝수로 라운딩 (round to the nearest, ties to even)
가장 가까우면서 0에서 먼 수로 라운딩 (round to the nearest, ties away from zero)
+∞를 향한 라운딩 (round towards +∞, rounding up, ceiling)
−∞를 향한 라운딩 (round towards −∞, rounding down, floor)
0을 향한 라운딩 (round towards −∞, truncation)

이진수
가장 가까운 짝수로 라운딩
=> 표준에 내정된 라운딩 규칙

십진수 실수 <=> 이진수 실수 변환
0  1000 1100  1011 1111 1010 0100 0000 000
s  E          F

e = E - bias = 140 - 127 = 13
𝑚 = 1 + 𝑓 = 1 + 0.101111111010012 = 1.101111111010012
𝑣 = (−1)0× 1.101111111010012 × 213 = 11011111110100.12 = 14324.510

−3.141595 × 1010 = −1 1 × 1.110101000010001001010110101011 × 2^34
“가장 가까운 짝수로 라운딩” 적용 후
(−1)^1 × 1.11010100001000100101011 × 2^34

𝑚의값은 소수부𝐹의 값을 𝑓라 할때, 𝑚=𝑓
• 서브노멀 값은 소수부의 바로 왼쪽에 소수점을 가정하나 정규 값처럼 암묵적인 1을 가정하지 않음

## 오버플로우워 언더플로우
언더플로우: 값이 너무 작음

## NaN

E = 11...1 이고 F≠0.00...0 일 경우

NaN 종류
QNaN(Quiet Nan)
SNaN(Signaling NaN)
컴파일러 옵션으로 지정

## 부동소수점 연산
결합법칙이 성립하지 않음
1.125 + 𝑟10^10 + 𝑟(−10^10) ≠ 1.125 + 𝑟(10^10 + 𝑟(−10^10))

덧셈/뺄셈
1. 소수점을 맞춤
2. 가수를 더함
3. 라운딩

곱셈
1. 지수를 더함
2. 가수를 곱함
3. 라운딩

나눗셈
1. 지수를 뺌
2. 가수를 나눔
3. 라운딩

• 대부분의 컴퓨터가 부동소수점 연산을 빠르게 수행하기 위하여 FPU(Floating-Point
Unit)이라 불리는 부동소수점 연산장치를 따로 가지고 있음
• 부동소수점 덧셈 및 뺄셈을 수행하는 기본적인 하드웨어
• 지수를 비교하는 하드웨어
• 지수의 차를 구하는 고정소수점 ALU
• 가수의 소수점을 맞추는 시프터
• 가수를 더하는 고정소수점 ALU
• 결과를 정규화 하는 하드웨어
• 라운딩을 수행하는 하드웨어
• ±0.0과 ±∞ 같은 특별한 값을 다루는 하드웨어
• 부동소수점 곱셈이나 나눗셈을 수행하는 하드웨어는 덧셈 및 뺄셈을 수행하는 하드웨어보다 상대적으로 간단

## Fused Multiply-Add
a x b + c
=> 한번의 라운딩만 적용해 빠르고 정확

왜 부동소수점 표현은 2의 보수 표현법을 쓰지 않는가..? sign 비트가 있는데 왜...

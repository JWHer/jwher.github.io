---
title: 'CUDA 프로그램 실행 과정'
draft: true
---

## CUDA 프로그램 실행 과정

<호스트에서 수행>
스트림(stream) 생성, 디바이스 메모리 할당
커널 코드 실행
입력 데이터를 디바이스 메모리로 전송 -> 디바이스
출력 데이터를 호스트 메모리로 전송 <- 디바이스

## CUDA Multi-GPU
1. 한 CPU 스레드가 여러 GPU 관리
2. 한 CPU 스레드가 한 GPU 관리

## CUDA API

사용 가능한 GPU 개수 확인
```cpp
​cudaError_t cudaGetDeviceCount(int* count)
```

CUDA API 의대상이되는GPU 지정
```cpp
cudaError_t cudaSetDevice(int device)
```

## 비동기 메모리 복사 유의점
• 메모리복사의경우비동기API를사용해도실제로비동기로동작하지않는경우가있음
• cudaMemcpyAsync(...)
• 비동기API가동기로동작하는두가지경우
1. 디바이스메모리↔ pageable 호스트메모리
2. 호스트메모리↔ 호스트메모리
• 비동기API 로데이터전송을할때는호스트메모리가pinned 메모리인지체크할것
• cudaMallocHost() API로pinned 메모리할당가능
• cudaFree() API로할당해제



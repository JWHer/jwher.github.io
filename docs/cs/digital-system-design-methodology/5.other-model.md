---
title: 'State-oriented model'
draft: true
---


시간에 따라 구분

Synchronous Model
- Synchronous-Reactive Model
Timed Model

시간이 모델의 일부
- Discrete-Event Model
- SIMULINK Model
Untimed Model

모델과 시간이 분리
- Dataflow Model
- Process Networks

Heterogeneous approaches

# State-oriented model

FSM, State-chart

# Activity-oriented model

## Synchronnous Model?
Synchronous Reactive(SR) Model

데이터플로우 모델과 비슷하나 피드백이 없음

동기화의 의미
- 시간축이 슬롯으로 구분

실제 작동 시간이 적음?

분류
- 완벽히 동기화된 MoC
- 클락 동기화

Synchronous Reactive Model은 무한히 실행된다고 가정.
feedback이 있을때?

ex) not gate(inverter)일때 
- zero processing: 중간 전압 (perfectly sync)
- clocked sync: 오실리에이션 -> 측정 어려움(unstable solution)

s->F->F(s)  
s=F(s)로 두고 fixed point 를 찾음
값이 있으면 well-formed, 아니면 ill-formed(no solution or multiple solution)
- Fixed point semantics
- Constructive Semantics

### 정리
- Fixed Point
- Cycle-driven
- Characteristics
= 컨트롤 집중적인 시스템에 적합
= 딱 맞게 동기화
= 많은 사례에서 결정
= 하드웨어와 소프트웨어에 잘 맵핑됨
- Limitation
= 적합하지 않다(무한루프) 피드백이 없으면 ddataflow와 비슷함
= 계산 집중적인 시스템이 과명세됨
= 루프감지가 어려움
= SW 구현이 힘들다

## Timed Model

Timed Model은 이산적인 이벤트 모델에서 사용, causallity 찾기 위해서

실질적으로 시뮬레이터에서 많이 쓰임. SIMULINK, SystemC

Cyber-physical system에서?

ex) 시간을 어디까지 명세해야 하는가?

a->b->c->d->e 10ms까지 끝내야 한다.

Descreate Event Model: 모든 컴퓨테이셔널 블럭에 데드라인을 주어야 함. Overspecification?
따라서 10ms를 컴퓨테이셔널 블럭에 어떻게 분배하는게 가장 중요한 문제가 됨

교수님 의견: e2e 시간만 맞추면 된다. 제한시간이 길어질수록 시간 분배를 할 필요가 없음

## Event Driven Simulation

시간 정보를 가진 이벤트들이, Global event queue를 두고 수행시간이 작은거부터 수행  
=> 소팅 오버헤드가 필요nlogn. 느림(SystemC)

> 왜 정렬이 필요한가? pin accurcate가 아니고 미래의 끝나는 시간을 따라가는 것이기 때문에 들어온 순서가 아닌 나가는 순서로 정렬?

Timed가 동작하는 방법
- 작업이 많을때 사용. global clock을 둠
- 모든 이벤트 큐 탐색(오버헤드 n), 데이터가 있으면 처리
- Global clock 증가
=> 대부분에 하드워어 시뮬레이션에서 빠름

결국 정렬할 요소가 얼마냐에 따라 다름  

Event-driven에서 정렬을 해결하는 방법 => clock wheel: k개의 슬롯(큐). 이벤트 시간에 따라 들어갈 큐를 미리 정렬해 둔다
n/k + logn/k

simultaneous event 문제
- VHDL은 clock time으로 정한 시간보다 작은 시간을 $\delta$로 표현. 하지만 $\delta$끼리 시간 비교는 어떻게?(조금 차이로 두번 실행되는 낭비가 있음)  
=>topological sort(같이 처리), fixed point computation

## SIMULINK

시뮬레이션 모델
- 코드 생성이 쉽지 않음

- 연속적인 시간 semantics
*piece-wise constant
식이 있고, 특정 시점을 샘플링 하는 것임
샘플 시간 = gcd(a,b)

- Triggered subsystem
=> 합성하기가 어려움, correct by consturction 보장이 안됨

- Enable subsystem

정합성? 문제로 하드웨어 소프트웨어 코디자인과 잘 맞지 않는다  
control oriented 시뮬 / compute oriented => dataflow

### E.G. H.264  
Dataflow 모델은 queue가 있다고 가정. 하지만 simulink가 없기 때문에 block에 구현

어려운점: matlab 코드외(c/c++/ada/fortran) import시 s-function
=> 디버깅 불가

# Untimed Model
시험에서는 잘 몰라도 된다
Concurrent processes model??

## Computation Graphs of Karp and Miller
(유명, 근데 여기선 안중요)

- A: # of token initially in the queue (=token?)
- U: # of token to be added after the input node is executed (=sample rate)
- W: # of token to be fetched for the output node to be fired (=?)
- T: minimum queue length

특징
- 실행 순서가 똑같다
- 종료 조건에 대한 정적 분석
- 스토리지 용량에 대한 정적 분석

=> SDF보다 superset, 병렬프로그래밍에 많이 사용

### Petri Nets

Place(=sdf queue?): 원으로 된 storage 같은
Transition(=sdf node): 선으로 된 것
Arc
M: initial marking

Turing complete 하지 않고 finite 하지 않음

Safeness, Boundedness, Conservative, Liveness

- finite reachability tree
w: the number of token grow arbitary
쉽게 탐색 가능

- Reachability problem

- Unsolvable problem

많은 바리에이션이 있음

## Marked Graph

## Kahn Process Networks
*KPN 중요*

동기화 없이 communication 하려면 infinite queue가 필요
(blocking read, non-blocking write 여서)
장점: determinate(실행 순서에 영향 받지 않고 독립적=>디버깅 가능), 멀티코어
단점: 쓰기 어려움(infinite fifo queue)?

*대조*  
CSP(concurrent sequential processing)
싱글 프로세서에서 동시성 문제
- Synchronous communication
=> rendezvous sequential method: 블로킹

KPN 특징
- easy to translate legacy c code to kpn
- deterministic execution
- execution policy
= demand driven execution: 가장 마지막 수행되는것부터 계산해 큐 크기를 최소화
= data driven execution: 큐 크기를 정할 수 없기 때문에, overflow시 block 하게 구현
- async input 안됨

### YAPI extension
Y-chart a p i

select api 추가
=> non-deterministic해짐

### Dataflow Process
select, merge(일부만 입력이 있어도 실행)

### Sequential Firing Rules
어떤 순서로 입력이 들어올지 정함

### KPN vs Dataflow

- KPN
blocking에 따라 context switch
large granuallity

- Dataflow
외부 스케줄러가 순서를 정해줌

Merits
- Loose synchronization (distributable)
- Determinate under simple conditions
- Maps easily to threads, but much easier to use
- Turing complete (expressive)

Limitation
- Control-intensive systems are hard to specify
- Resource requirement can not be predicted

# Heterogeneous Model

control과 compute 모델을 다른 걸 쓸 수 없을까?
=> 계층적으로 만들자!

## Ptolemy
c

warmhole 매커니즘(다른 형태의 모델이 계층적으로 나타남)

## Ptolemy2
java+engineering

directory: dafaflow / fsd / 심지어 finite state machine도..

internal event로 concurrency 지원  
interaction..  

State mate, activity chart

- Compositionality
- Heterogeneity
- Termination

## Dataflow with FSM

## Heterochronous Dataflow with FSM

In HDF, an actor has a finite number of type signature.
- Unlike CSDF, the order of which type signatures are used is not predictable.
- Still key analytical properties can be statically determined.
- Deadlock and bounded memory

## Discrete Events with FSM

## Hopes
- top level
- each task level

Control Task: supervisor

Send a request to the "hidden" supervisor through a system port.

Computation Task: ?

## SADF: Sinario-Aware Dataflow Graph
네덜란드

mode transition machine(=finite state machine)
다른점은 어떤 출력도 나오지 않음(finite state machine의 제한된 버전)

각 모드는, fsm의 한가지 패스  
mode 변경을 위해(=다른 패스를 타기 위해) sample rate를 조절  
static check가 가능

# 문제풀이 
식풀기 y=-y(y+4)
discreate event의 단점: 정렬과, simultaneous event 문제(시뮬레이션 단위 시간보다 작을때)
=> 시뮬레이션에는 좋은데 왜 합성에는 이용하지 못할까??
Simulink의 연속적인 시간:
PeaCE와 STATEMATE 차이:
KPN은 비동기적인 이벤트를 받을 수 없고 YAPI
두개의 상호작용 매커니즘 FSM SDF가 Hopes에서

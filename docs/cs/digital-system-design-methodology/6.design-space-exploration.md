---
title: 'Block Performance Estimation'
draft: true
---



리마인드: Y chart approach for DSE

1. 알고리즘/기능 시뮬레이션
2. 하드웨어 소프트웨어 파티셔닝, 베스트 매칭 찾는게 DSE
3. 매칭이 되면 파인튜닝
4. 검증
5. 합성

# Block Performance Estimation

Processing block에 actor 할당하는 법  

## Block Execution Time Estimation

결국 성능예측이 필요함
Performance Estimation Method
- simulation based approach
=> average, slow.
- analytical approach
=> worst case, Fast. Static performance estimation에서 자세히 다룸
- Hybrid approach

Ni : execution count of Block i
ti :  CPU performance
sni : non sequential read miss counts
spi : non sequential read miss penalty
nni : sequential read miss counts
npi : sequential read miss penalty

### 남은 문제
CPU와 Memory,
하지만 블록의 앞/뒤 코드에 따라 달라질수(오차의 가장 큰 원인?)

(recall) write through 캐시. write buffer가 비어있으면 no penalty.

best representative number
- average, worst, best

## Power Estimation

$$
D = \alpha f C v^2
$$
D: power dynamic
alpha: ?
f: ?
C: ?
v: voltage

### Stanford 방법
C를 찾는법이.. 데이터시트?

### System-level Power Estimation

E_{system} = E_{cpu} + E_{memory} + E_{bus} + E_{static}

E_{cpu} = P_{active} * T_{active} + P_{deactive} * T_{deactive} => from datasheet

# HW/SW Cosynthesis

## Partitioning Problem
problem space -> solution space(고정된 알고리즘)
플랫폼 개발자 / 사용자 관점

- heuristic
List scheduling: bottom부터 worst case 탐색(critical path). critical path에 첫 시작부터 차례로 매핑
단, 동일 프로세서에서만 가능

(BIL): Best Imaginary Level, BIM
BIM scheduler: 이종 프로세서 가능

- meta-heuristic
Genetic algorithm
Chromosome – Blueprint for an individual 
Genome – Collection of chromosomes  
fitness evaluation  
mutation: exploitation, crossover: exploration

- mathmatical programming

## Objective/Cost Functions
종류가 다양한 이유:
different objective

## Pruning the Design Space

ex) genetic algorithm, simulated annealing?

커뮤니케이션 아키텍처는 idle 하다고 가정하고 pe를 먼저 찾음.

Partitioning Problem Gets Harder...
- Task level (가장 많이 연구)
- Data (한번에 여러개 pe에 매핕)
- Temporal (파이프라이닝)

# Communication Architecture Exploration 

NoC(Network On Chip)
다양한 컴포넌트가 칩에 있어 서로 연결하는게 중요해지고 있음
Mesh, Tree(fat), Bus(less than 10 pe)

결국 통신은 메모리 구조가 영향을 많이 줌

- Static estimation based approach
시간 빠르나 부정확
- Simulation based approach
정확하나 시간 걸림
- Hybrid Approach

## Bus Architecture
가장 간단하고 많이 쓰이는 방법

### 버스 토폴로지

### Exploring Bus Operation Conditions
Bus operation conditions for a given bus topology
- Data-width
- Operation clock frequency
- Priority assignment of processing elements

### Static Performance Estimation
Queuing Model. queue size. bus contention

# summary

PE와 communication을 분리해 탐색.

Iterative two-step exploration technique
- 1st step: Rapid reduction of the large design space using static performance estimation
=> 가지치기
- 2nd step: Accurate performance evaluation on the reduced design space
- The validation with two preliminary applications

1. Explain a popular method of estimating the system power.

2. What is “pareto optimal design”? . 

3. Compare the HW/SW partitioning techniques. 
hueristic/meta-huerisitc

4. Explain the hybrid approach for design space exploration of communication architecture. 
static/smulation

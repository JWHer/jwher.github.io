---
title: 'Introduction: Dynamic Behavior Specification'
draft: true
---

INTRODUCTION TO EMBEDDED SYSTEMS
A CYBER-PHYSICAL SYSTEMS APPROACH

## Introduction: Dynamic Behavior Specification

Cyber Physical System

Cyber Physical + Embedded System

### Remind: Actor model of systems

## Discretized Model: A Step Towards Software

G-transform (discretized time system)

Understanding models can be very challenging

## Garage Counter(e.g.)

Guard

> Finite State Machine
> moore type(stateless, only input), mearly type(state)

## FSM Mathematical Model

그러나 컴퓨터는 이걸 이해 잘 못하기 때문에 표현을 바꿔주어야 함

Q :  finite set of symbols denoting states
$\Sigma$ :  set of symbols denoting the possible inputs 
\Delta :  set of symbols denoting the possible outputs
\sigma : transition function mapping
q_0: initial state

\epsilon: no output

Automata와 유사
- Deterministic FSM
- Non-deterministic FSM

환경에 따라 Non-deterministic일때(예, 자동차가 오면 파란불이 되는 신호등)
환경 모델링도 필요

=> 비결정적이면 복잡해지므로 결정적으로 바꾸는 것이 중요

## Extended State Machines

다양한 것이 있음. 간단하게 해야 함

## Extended state machine: a  traffic light controller

pending이 없으면 매 초마다 pedestrian 이벤트 발생

## When does a reaction occur?
- time triggered
- event triggered

# Composition of State Machines

## Side by Side Composition

$S_C = S_A \times S_B$
- Synchronous composition: 한번에 하나씩이기 때문에 
- Asynchronous composition: 모든 조합이 가능


## Cascade Composition

synchronous composition

1. 모든 상태를 조합하고
2. transition arc를 그리고
3. 도달 불가능한 상태를 보고
4. 제거한다

## Feedback Composition

## Hierarchical State Machines
- History transition: subsystem의 state를 기억해야 함
- Reset transition: 기억할필요 없음

subsystem과 supersystem transition이 같이 오면
- Inner-FSM-first: 하위 state도 진행
Simultaneous transition must NOT conflict.
- Preemptive Semantics: 상위 state만 진행

## Harel’s Statechart

- FSM Hierarchy *중요
- concurrency

부족한 단점을 어떻게 보완하는가?

한번에 몇개의 transition을 허용할것인가?
- 1. Prohibit instantaneous transition
- 2. Allow instantaneous transition with some limitation
= unless make cycle

### AND Decomposition

### Esterel

# FSM-based Design Environment

## Activity chart

- transformation
- data-store
- control activity

- dataflow
- controlflow

A[C] / B
A: event: primitive or special
C: condition
B: action: primitive or special

## Executable Specification

Correct-by-Construction

## POLIS: Codesign FSM

산조바니
CFSM

# Static Analysis

## Temporal Logic
express timing

## Propositional Logic
연산자 똑같음

## Propositional Logic on Traces
same as execution trace...

A propositional logic formula p holds for trace

## Linear Temporal Logic(LTL)
시간의 변화에 따라 작동을 검증하기 위해

트레이스에서 linear temporal logic 검사

### P(reposition)
p를 만족하는 trace

### G(lobal)
모든 상황에 대해 성립

### F(inally)
결국 성립한다

### X(next)
다음 상태에 성립한다

### Operator U(ntil)
조건이 만족할때까지 성립한다

p_1 U p_2
=> p_1이 p_2가 트레이스의 후속을 가질 때까지 p_1이 이전 항목을 들고 있음

1. G F p
p holds infinitely often
p가 계속 나오므로...

2. F G p

3. 

Examples: 
“Whenever the iRobot is at the ramp-edge (cliff), eventually it moves 5 cm away from the cliff.”
p - iRobot is at the cliff
q - iRobot is 5cm away from the cliff

G(p=>Fq)

“Whenever the reset signal is asserted the state machine shall move immediately to the ErrorReset state and remain there until the rest signal is deasserted”
p – reset signal is asserted
q – state of the FSM is ErrorReset

G(p=>X(q U not p))

서로간에 관계
- G 변환
Gp = not(F not p)
p가 나타나는것은 결국 p가 나타나지 않지 않은것

- F 변환
Fp = true Up

- X 변환
x

Lee and seshia's book


## Comparing State Machines

같음을 판단하는 기준, ~state와~ output이 동일

1. type
입력/출력(포트)가 같음

2. language
automata(언어 생성기)  
입력/출력 trace 결과가 똑같음

3. Bisimulation
둘다 시뮬레이션 가능한 것, in any case

deterministic일때 lang equivalence하면 sim equivalence함
non-deterministic일때 type/lang 결과가 다를 수 있음

### Equivalence vs Refinement
일종의 subset

### Type Refinement
입출력을 무시함으로써 동작은 할 수 있음

입력 개수 p1>p2, 입력 타입 p2>p1
출력 p2>p1, 출력 타입 p1>p2

ex) garage counter M1:99대, M2:90대

### Regular language
(automata)
accepting state(final state) 표시가 되려면 어떻게 생성해야한다

### Omega regular language
buchi automation = 안중요
automata로 변환해서 찾을 수 있다는 내용

### Language Refinement
생성 가능한 M1보다 subset인 M2이면 됨

ex) m1은 m2를 시뮬레이션 가능하지만, m2는 m1을 시뮬레이션 할 수 없음
subset(m2)이 simulation refinement

The matching game

m1은 보통 스펙, m2는 구현

## Sumary
2 way of formal verification

-trace
-compare state machine

기본적으로 exponential 한 작업이여서 이걸 줄이는게 중요

도구: promela, SPIN 모델 체커

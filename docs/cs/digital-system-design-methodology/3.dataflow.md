---
title: 'Model and Design Environment'
draft: true
---

paper: A Framework for Comparing Models of Computation

## Model and Design Environment

### Design is to represent

### Importance of model

모델이 결정되야 한계가 정해짐

### Status

명세 없이 디자인 공간이 존재하지 않음?

## Model Classification

## Model of Computation

# Dataflow Model

directed graph

## Buffers for Dataflow 

ring buffer

## SDF (Synchronous Data Flow)

CDFG: control data flow graph(Any compiler) = controled by compiler
Data flow computer = contoled by machine, only have data deps, 그러나 shared resource 정의가 없어서 실패(비용높음)

stric fuction: 모든 매개변수 올때까지 대기 (SDF)
non stric function: 함수 실행하고, 다른 매개변수 필요할때까지 실행 정지

SDF model has fixed sample rates on all arcs
- 입력 데이터는 고정됨(static schedule?)

> static data flow가 되어야 하지 않았나 - E.A. Lee

A dataflow model with strict firing condition
- 조건(conditional) 실행을 직접적으로 지원하지 않음

We can determine the static schedule of the graph
- 예상가능(수행시간, 메모리 사용량)

### Two Implementations of Multi-rate
모델과 다른 것임. 구현하기 나름

- Multiple clocking (DSP Station)
A - 1B4 - C  
B가 4번 time clock 할때동안 기다림

homogeneous

- Multiple Samples (Ptolemy/PeaCE)
B가 한 클락에 4번 샘플 만듬

synchronous

### Static Scheduling

- 노드 반복 수 비율이 관리되어야 함
- 최소 사이클이 최소 노드 반복 수 비율을 맞춰야 함

버퍼 크기는 이웃의 LCM(least common multiple)

What is the best schedule?
- 디자인 목적에 따라

## PASS: Periodic Admissible Sequential Schedule

## Syntax check
- Sample rate inconsistency
- Deadlock

## SDF Limitations
- Expression Capability

- SDF model does not allow shared memory (global states) between blocks due to side effect
메모리 갱신이 스케줄 순서에 따라 이루어지기 때문

- SDF model does not allow pointer operation, but copies structured data as a token
가상화에 적합하지 않음

# Software  Synthesis from SDF Model

- New trend of software development : model-based design
growing complexity, fast  design turn-around time, 

- Automatic code generation from data flow graph 
SDF semantics should be preserved - “refinement”
The kernel code of a block is already optimized in the library.
Determine the schedule and coding style.
Codes are generated according to the scheduled sequence 

- Fundamental Question
Can we achieve the similar code quality as manually optimized code in terms of performance and memory requirement?

## Software Synthesis

## 코딩 스타일

## 메모리 요구량

메모리 요구량은 코딩 스타일에 많이 달림

- Code memory: depends on coding style
Inlined-style, Switch-style
 SAS (Single Appearance Schedule) is preferred.
In case of no-SAS, define a function in the body. 
Good for fine granule node
Code sharing is helpful if the block context is smaller than the code size

- Data memory: depends on schedule & buffer sharing

### 버퍼 메모리 요구량
스케줄에 따라 다름.  
각각 요구량에 합으로 구할 수 있음  
버퍼 공유를 통해 생명주기가 다른 버퍼를 공유하면 크기를 줄일 수 있음

=> 스케줄에 따라 버퍼의 크기를 많이 줄일 수 있음
=> 코드 오버헤드도 고려해야 함

### Buffer Memory Lower Bound

### Optimal Schedule for Chained Graph

no-buffer sharing: Dynamic programming: O(n3)

### Extensions
Buffer minimization problem is NP hard for general graphs -> heuristics

### Buffer sharing
seperate global buffer / local buffer

1. global buffer minimization
2. local buffer 
3. repetition

# Parallel Scheduling

## Parallel Scheduling Algorithms
NP Complete

Many heuristics have been developed (mainly for homogeneous task graph)

SDF-to-APEG (Acyclic Precedence Expanded Graph) 

## Arc buffer size affects the throughput!
병렬화를 하려고 해도 버퍼 사이즈에 제한받을 수 있음  
=> 여려 프로세스로 나뉘게 됨, 실행하는데 문제는 없지만 코드 매핑과 관리가 어려움  
=> 요즘은 SDF 자체로 찾는 방법 연구

## Unfolding affects the throughput!

static mapping으로 folding 가능

## Scheduling Techniques
- fully static
- self-timed static: 실행 순서는 고정되나, 시작은 고정되지 않음
=> 다음 항목을 바로 시작하려면 데이터 도착을 확인해야 함
- static mapping dynamic scheduling: 프로세서에 작업이 고정적으로 할당되나, 종속된 작업이 빨리 끝나면 실행 순서는 바뀔 수 있음
=> 스케줄링 알고리즘이 필요(self-timed는 global knowledge가 있으나, static mapping은 로컬 디시전 필요)

## A Problem Definition
- minimum buffer size
- priority of the mapped nodes

=> GA(Genetic Algorithm)을 사용 NP hard

JGAP package, Chromosome(total solution) and gene(solution). fitness(object func)

# Static Analysis of Throughput 

## Analysis Procedure

MCM(Maximum Cycle Mean): easy to apply
=> 사이즈카 커지면?

=> acyclic 이면 throuput bound가 없음(무한)
=> cyclic이면 throuput = sum(a,b) / num_of_sample
=> 여러 cycle이 있으면 그중에 가장 작은 값?

## State Space Exploration Method

# Decidable Dataflow Graphs – SDF extensions

## SDF Limitations

- Expression Capability
- Performance issues
- Multi-dimensional extension

## CSDF (Cyclo Static Data Flow)
Sample rate

장점
- Even possible to form a deadlock-free feedback-loop without any delay on the arc
- CSDF gives better chance of actor composition
- CSDF requires smaller buffer size than SDF

### Translation: CSDF to SDF

- SDF is a special case of CSDF: the period of all nodes are 1 
(csdf가 superset)

- Sample rate consistency can be checked with the translated SDF

- The translated SDF graph can be deadlocked while the original CSDF graph is not

number of initial sample(sdf) = delay(csdf)

### Even possible to form a deadlock-free feedback-loop without any delay on the arc

### CSDF gives better chance of actor composition

### CSDF requires smaller buffer size than SDF

## SDF Extensions: FRDF

부분적인 데이터 플로우를 허용 -> 메모리 사이즈 감소

- Fractional number of samples can be produced or consumed
- Reduce latency and memory size
- Well match with multimedia applications 
Reduce the buffer size by 70%  compared with SDF
- Future plan
Buffer-optimal schedule

그러나 granuallity가 증가하면 쓰기 힘들다..?

## BDF

## Dynamic Dataflow (DDF) Model

- data dependent

## Library Task



---
title: 'WCET analysis of a software task'
draft: true
---


rule of thumb?
worst case를 보통 1.5배로 잡음, 하지만 이건 보장되는것이 아님

- System level estimation
- Component level estimation(processor, communication bus)
- Process timing analysis(WCET time analysis) 

# WCET analysis of a software task

## Components of Execution Time Analysis
- Program path (Control flow) analysis
- Processor behavior analysis
- Outputs of both analyses feed into each other

## The WCET Problem

Integer Linear Problem

# Component-level Timing Analysis
wcet이 주어졌다고 가정했을 때
- processeor
- communication bus

Dynamic: EDF, RM  
Time-driven: TDMA(static), round-robin(dynamic)

## Comparison between EDF and RMS

Favoring RMS
Scheduling decisions are simpler (fixed priorities vs. the dynamic priorities required by EDF. EDF scheduler must maintain a list of ready tasks that is sorted by priority.)
Favoring EDF
Since EDF is optimal w.r.t. maximum lateness. For infeasible schedules, RMS completely blocks lower priority tasks, resulting in unbounded maximum lateness.
EDF can achieve full utilization where RMS fails to do that
EDF results in fewer preemptions in practice, and hence less overhead for context switching.
Deadlines can be different from the period.

대부분 real-time 접근법

### RM response time
### RM w/ arbitary deadline
### Jitter

## TDMA
Response time
utilization이 낮아서 자동차같이 비용이 중요한곳은 잘 안씀..

$t_{TDMA}$ 전체 타임 슬롯
$t_{Pi}$ 내가 차지하는 슬롯

$R_i = C_i + (t_{TDMA} -  t_{Pi}) \times \lceil {C_i \over t_{Pi}} \rceil$

## SDF schedule
Fully static == Giotto model
빨리 끝나도 기다림. arrival time이 아닌 time(period)에 실행

# System-level Analysis

## Global System Level Analysis

sum of worst case is not end-to-end worst case
- Communication(Bus)/Shared resource
=> TDMA는 이런 일이 없어 이점

### Holistic Schedulability Analysis

커뮤니케이션 오버헤드 식을 추가. 하지만 특정 시스템에만 적용가능하다는 단점.

scalable 하지 않음.

### Compositional Schedulability Analysis

시스템을 모델링하고, 버스를 사용해 scalable 하게 만듬.

커머셜 솔루션: Symta vision(회사)

- SymTA/S
*이벤트 스트림 모델(p,j,d)*
p:period, j:jitter, d:minimum distance

이벤트가 비주기적으로 올때(sporadic task): period를 평균을 내서 구함

Event propagation: p, j, d가 어떻게 변하게 될지 예측

* Conventional approach
p,j=(50,0) => CET=[2,8] p,j=(50,6) =>
jitter에 case execution time의 차이를 더함, 여러개일때는 높은 우선순위 사용, 낮은 우선순위는 높은 우선순위의 excution tiem 더함

Inter-context deps: 피드백 루프가 있으면 jitter가 길어짐

* Tindell
jitter를 줄일 수 있음

Consideration of Cyclic Deps: 반복으로 수렴확인

- MPA(Modular Performance Analysis)
전화와 같이 너무 jitter가 큰 요소들

arrival curve, service curve

load model, service model, processing model

* Real-time calculus
Max-Plus/Min-Plus Algebra 
network calculus

* load model
burst: jitter is bigger than period.

(p,j,d) -> arrival curve는 가능, 반대는 안됨  
따라서 MPA가 더 정확하다고 해도 SymTA/S를 분석에 사용 가능

* service model
C(t) 실행에 필요한 커뮤니케이션 사이클

* Abstract Component Model
load model + service model = 복잡해짐

## Timed automata based method

WCRT

UPPAAL

## Schedulability Analysis

- STBA (Scheduling Time Bounad Analysis)
Better than ILP...

(best, worst) pair
min Finished Time -> min Start Time
이전 작업의 최소 끝나는 시간은 다음 시간의 최소 시작 시간과 같다

max Start Time이 다음 min Start Time과 겹치지 않는지.
겹치는(Interfere) 시간을 찾아야 함

=> Fast and Accuracy 하지만 Task가 같이 시작된다는 가정이 필요함(no offset)
=> offset이 적당히 작으면? time bound로 할 수 있음

offset이 0인 task graph는 하나의 큰 task graph로 합칠 수 있음

- ILP: Integer Linear Programming based

Objective Function 

MILP Objective function for finding WCRT of 
=> not practical(RTAS) but theoretical(RTSS)

- SWAT (SNU WCRT Analysis Tools)

Easy of use: modeling complexity – {SymTA/S, MPA, HPA}, UPPAAL 
Speed – MPA, SymTA/S, HPA, ILP, UPPAAL
Composability/extensibility – {SymTA/S, MPA, HPA}, UPPAAL, holistic
Accuracy (tightness of bound) – {UPPAAL, ILP}, HPA, {MPA, SymTA/S}

HPA - good for commertial

*STBA, HPA, RTA*
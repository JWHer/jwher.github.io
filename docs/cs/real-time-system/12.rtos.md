---
title: 'Xenomi'
draft: true
---

# Xenomi

locking & unlocking 

문제: naive(low task가 high task block, priority inversion. 여기서 middle task가 low task pre-emption함)

## Mutex functions

### Time-bound trylock
rt_mutex_acquire_until
=> 특정 시간 이상 release가 안되면 다른 걸 함, priority inheritance 코드가 포함되어있음(kernel)



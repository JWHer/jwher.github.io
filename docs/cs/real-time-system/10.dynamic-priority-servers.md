---
title: 'Constant Utilization Server'
draft: true
---

EDF면 absolute deadline

## Constant Utilization Server
(CUS)

e.g. 50% utilization

D(eadline) = e(xecution) / u(tilization)

A1(2)
A2(3)
<2>--|<3>---|

Rule1. e=0, d=0

Rule2. an anperiodic request e arrives at time t_s
A) Queue is not empty, join the queue
B) pending if t<d. 데드라인 전까지 버짓 생성 안함. => 버짓을 t에 먼저 생성하고, max(t,d_s)로 바꾸면 Total Bandwith Server(TBS) = Deadline은 유지됨
C) d_s=t+e/u; e_s=e; d_s는 현재 데드라인. 데드라인 갱신

Rule3. when t becomes equal to d_s
A) d_s = d_s + e/u_s; e_s=e.
B) do nothing

## Total Bandwith Server

### Feast and Famine Under TBS

CUS에서는 없는 문제

=> idle time 일때 deadline을 무시하고 새 작업이라고 생각할 수 있음 (idle time remma)

문제: aperiodic task의 execution time을 알 수 있는가?(deadline을 정해야 함) => wcet?

aperiodic arrives at time t_s, budget c_s = Q_s(e를 모르기 때문), deadline d_s=t+P_s
버짓이 남으면 다음꺼 진행
버짓이 없으면 다시 queue에 들어가서 진행해야 함.

## Sporadic Task
firm deadline

instantaneous utilization(몇개 작업이 queue에 있는가??)

r_i - d_i
e_i

J_i = e_i / (d_i - r_i)

Sporadic Job Theorem: sum of all active jobs is small than 1 at all time t.
=> neccessary condition

Online admission test:
- job arrives, add instant utilization. if peroid takse 70% Utilization
- 
- 

---
title: '갈로아 연결 정의:'
draft: true
---

프로그램 정의
[[C]] = U F \bot

CPO, U(합집합), \vot, 드

fix F, 연속함수

*요약해석(abstract interpolation)

유한하지 않을 수 있음! (not finite)
-> widening operator 사용. 이는 프로그램마다 다름

요약 해석으로 분석하기:
1. 프로그램의 실제 실행의 정의
2. 프로그램의 요약 실행의 정의
3. 올바른 요약 실행인지 확인
4. 요약 실행을 계산하는 방법


실제 실행: $[\![C]\!] = fix F \in D$  
요약 실행: ${[\![C]\!]}^{\#} = lim_{i\in\mathbb{N}} F^{\#i}(\bot_{D^{\#}})$

F^#, D^#은 자유로우나 다음 관계를 만족
F^# \in D^# -> D^#
즉 F^#은 단조 함수거나 팽창 함수이다.

° 1:½¨¹כ

요구1.
갈로아 연결(Galois connection)
D와 D# 은 갈로아 연결

갈로아 연결의 정의

갈로아 연결의 의미
\alpha는 실체를 요약(abstraction)
\gamma는 요약한 원소의 실체를 정의(concretization)

예를 들어, 요약은 양수/음수 upper/lower bound등 필요한 것만 추리는 것

요구2.
F#은 단조 또는 팽창함수

요구3.
F3은 F를 포함

\alpha F \gamma


## 갈로아 연결 정의:
∀x ∈ D, x] ∈ D
]
: α(x) v x
] ⇐⇒ x v γ(x
]
).
### α는 최소를 보존한다(strict): α(⊥) = ⊥]
.
Proof. α(⊥) v ⊥] 왜냐면 ⊥ v γ(⊥]
).
### id v γ ◦ α.
Proof. α(x) v α(x) 이고 갈로아 연결로 x v γ(α(x)).

### α ◦ γ v id.
Proof. γ(x
]
) v γ(x
]
) 이고 갈로아 연결로 α(γ(x
]
)) v x
]
.

### γ 는 단조(monotonic) 함수이다.
Proof. x
] v y
] 라면 α(γ(x
]
)) v y
]
, 따라서 갈로아 연결로
γ(x
]
) v γ(y
]
).
